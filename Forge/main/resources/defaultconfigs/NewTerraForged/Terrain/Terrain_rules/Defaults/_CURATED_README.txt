Hand-authored ideal biome defaults for NewTerraForged.

These by_id JSON files are generated by tools/generate_curated_defaults.py
from an explicit catalog (every biome is a written dict).

Resolution order still prefers player Biomes/*.json, then Defaults/by_id,
then by_name templates. BiomeRuleAutogen is emergency-only.

Do not treat autogen output as curated truth — edit the Python catalog
and re-run the generator instead.
