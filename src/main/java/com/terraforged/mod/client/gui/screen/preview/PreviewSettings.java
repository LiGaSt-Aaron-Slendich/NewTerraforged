package com.terraforged.mod.client.gui.screen.preview;

/** Controls for the live preview map (scaffold subset of 0.2.x PreviewSettings). */
public final class PreviewSettings {
    public int zoom = 68;
    public RenderMode display = RenderMode.BIOME_TYPE;
    public boolean showCoords = true;
}
