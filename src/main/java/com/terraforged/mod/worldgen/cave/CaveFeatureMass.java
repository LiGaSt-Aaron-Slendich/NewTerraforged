package com.terraforged.mod.worldgen.cave;

/** Local mass kind for cave feature planning (KEEP). Not DROP FeatureMass / budgets. */
public enum CaveFeatureMass {
    BLOCKED,
    LARGE,
    MEDIUM,
    SMALL,
    SCATTER
}
